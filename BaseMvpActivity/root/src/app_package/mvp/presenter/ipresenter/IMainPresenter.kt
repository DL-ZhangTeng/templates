package ${packageName}.mvp.presenter.ipresenter

import ${packageName}.mvp.base.IPresenter
import ${packageName}.mvp.model.imodel.${imodel}
import ${packageName}.mvp.view.${view}

interface ${ipresenter} : IPresenter<${view},${imodel}> {

}