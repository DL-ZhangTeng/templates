<?xml version="1.0"?>
<template
        format="5"
        revision="5"
        name="Activity List(SmartRefreshLayout)"
        description="Creates a new ListActivity."
        minApi="11"
        minBuildApi="11"
        requireAppTheme="true"
        category="Activity">

    <dependency name="android-support-v4" revision="8"/>

    <category value="Activity"/>
    <formfactor value="Mobile"/>

    <parameter
            id="activityClass"
            name="Activity Name"
            type="string"
            constraints="class|unique|nonempty"
            default="ListActivity"
			suggest="${layoutToActivity(activityLayoutName)}"
			help="The name of the activity class to create"/>
	
	<parameter
		id="activityLayoutName"
		name="name(layout)"
		type="string"
		constraints="layout|unique|nonempty"
		suggest="${activityToLayout(activityClass)}"
		default="activity_list"
		help="The name of the layout to create for the activity" />
		
	<parameter
        id="isLauncher"
        name="isLauncherActivity?"
        type="boolean"
        default="false"
        help="If true, this activity will have a CATEGORY_LAUNCHER intent filter, making it visible in the launcher" />
	<parameter
		id="entityName"
		name="Entity"
		type="string"
		constraints="class|nonempty"
		suggest=""
		default="Entity"
		help="The name of the Entity to create for the Listactivity" />
	<parameter
        id="isNoDataView"
        name="isNoDataView?"
        type="boolean"
        default="false"
        help="If true, this activity will have a NoDataView" />
	<parameter
        id="isTitleBar"
        name="isTitleBar?"
        type="boolean"
        default="false"
        help="If true, this activity will have a TitleBar" />
	<parameter
            id="packageName"
            name="Package name"
            type="string"
            constraints="package"/>

    <!-- 128x128 thumbnails relative to template.xml -->
    <thumbs>
        <!-- default thumbnail is required -->
        <thumb>activity_fastlist.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl"/>
    <execute file="recipe.xml.ftl"/>

</template>
