<?xml version="1.0"?>
<template
    format="5"
    revision="5"
    name="HttpUtils(retrofit)"
    minApi="9"
    minBuildApi="14"
    description="Creates a new template for HttpUtils">

    <category value="Utils" />
    <formfactor value="Mobile" />

    <parameter
        id="isEncrypt"
        name="isEncrypt"
        type="boolean"
        default="false"
		description="Creates a new template for a encrypt HttpUtils"
       />
		
    <!-- 128x128 thumbnails relative to template.xml -->
    <thumbs>
        <!-- default thumbnail is required -->
        <thumb>template_blank_activity.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
