<?xml version="1.0"?>
<template
    format="5"
    revision="3"
    name="Fragment (CourseSchedule)"
    description="Creates a base CourseScheduleFragment that is compatible back to API level 4."
    minApi="7"
    minBuildApi="8">

    <category value="Fragment" />

    <dependency name="android-support-v4" revision="8" />

	 <parameter
        id="isScroll"
        name="Create a sroll CourseScheduleFragment?"
        type="boolean"
        default="false"
        help="Create a a sroll fragment to CourseScheduleFragment" />
		
    <parameter
        id="className"
        name="Fragment Name"
        type="string"
        constraints="class|nonempty|unique"
        default="CourseScheduleFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="fragmentName"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|nonempty|unique"
        default="fragment_course_schedule"
        visibility="true"
        suggest="fragment_${classToResource(className)}"
        help="The name of the layout to create" />

    <parameter
        id="includeNewInstance"
        name="Include fragment newInstance methods?"
        type="boolean"
        default="false"
        help="Generate static fragment newInstance methods for easy instantiation" />

    <thumbs>
        <thumb>template_base_fragment.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
